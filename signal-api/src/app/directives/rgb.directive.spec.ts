import { RgbDirective } from './rgb.directive';

describe('RgbDirective', () => {
  it('should create an instance', () => {
    const directive = new RgbDirective();
    expect(directive).toBeTruthy();
  });
});
